spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/Anubhavanandrai/FitnessAI_Config"
          username: ${gitUsername}
          password: ${GithubToken}
server:
  port: 8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

